#include <stdio.h>

    int main(){
        int faces = 6, arestas = 12;
        int vertices;

        vertices = arestas + 2 - faces;
            printf("O numero de vertices e %d", vertices);

    }
