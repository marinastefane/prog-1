#include <stdio.h>

    int main(){
       
            for (int num = 0; num <= 1000; num++)
            {
                if (num % 2 == 0)
                {
                    printf("\nvalor:%i", num);
                }
                 
            }
    }
